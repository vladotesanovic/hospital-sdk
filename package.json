{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@types/node": "^10.10.2",
    "lerna": "^3.4.0",
    "typescript": "^3.0.3"
  },
  "scripts": {
    "publish": "lerna run tsc && lerna publish"
  }
}
